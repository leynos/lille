[package]
name = "lille-ddlog"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
differential_datalog = { path = "differential_datalog" }
